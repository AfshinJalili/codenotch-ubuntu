# Agent notes

Reload the extension after `extension.js` edits on Wayland:

```sh
make reload
```

Do not trust `make dev-link` unless `codenotch@local` is a symlink to this repo, not a directory with a nested symlink inside.

Regenerate README demo assets:

```sh
make demo-assets
```
